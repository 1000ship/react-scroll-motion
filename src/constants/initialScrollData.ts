import { ScrollData } from "../types";

const initialScrollData: ScrollData = {
  currentY: 0,
  viewportHeight: 0,
  totalPage: 0,
  totalHeight: 0,
  totalProgress: 0,
  realPage: 0,
  currentPage: 0,
  currentProgress: 0,
};

export default initialScrollData;
